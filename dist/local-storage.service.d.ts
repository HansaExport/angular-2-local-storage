import { Observable } from 'rxjs';
import { ILocalStorageEvent } from './local-storage-events.interface';
import { ILocalStorageServiceConfig } from './local-storage.config.interface';
export declare class LocalStorageService {
    isSupported: boolean;
    errors$: Observable<string>;
    removeItems$: Observable<ILocalStorageEvent>;
    setItems$: Observable<ILocalStorageEvent>;
    warnings$: Observable<string>;
    private notifyOptions;
    private prefix;
    private storageType;
    private webStorage;
    private errors;
    private removeItems;
    private setItems;
    private warnings;
    constructor(config: ILocalStorageServiceConfig);
    add(key: string, value: any): boolean;
    addWithPrefix(key: string, value: any, prefix: string): boolean;
    clearAll(regularExpression?: string): boolean;
    clearAllWithPrefix(prefix: string, regularExpression?: string): boolean;
    deriveKey(key: string): string;
    get<T>(key: string): T;
    getWithPrefix<T>(key: string, prefix: string): T;
    getStorageType(): string;
    keys(): Array<string>;
    keysWithPrefix(prefix: string): Array<string>;
    length(): number;
    lengthWithPrefix(prefix: string): number;
    remove(...keys: Array<string>): boolean;
    removeWithPrefix(...keys: Array<string>): boolean;
    set(key: string, value: any): boolean;
    setWithPrefix(key: string, value: any, prefix: string): boolean;
    private checkSupport();
    private setPrefix(prefix);
    private setStorageType(storageType);
    private setNotify(setItem, removeItem);
}
